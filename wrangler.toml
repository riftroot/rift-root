name = "rift-bifrost-demo"
main = "worker/index.js"
compatibility_date = "2025-04-01"
account_id = "a62c1c7880b50ac345fc7c2135f6ae84"

workers_dev = false

[assets]
directory = "./public"
binding = "ASSETS"
not_found_handling = "single-page-application"
run_worker_first = true

# Custom domain bifrost-demo.mock1ngbb.com is attached via the
# accounts/.../workers/domains API (not via [[routes]]), because the
# CF_WORKERS_TOKEN doesn't carry zone:edit. Re-attach is idempotent
# through the API; this stanza is intentionally omitted.
